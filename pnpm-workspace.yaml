packages:
  - "apps/*"
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  argon2: true
  prisma: true
  # Resolver nativo (dep transitiva de jest). No requiere build para los tests
  # de Slotify; se deja sin compilar para mantener verde el deps-check de pnpm.
  unrs-resolver: false
