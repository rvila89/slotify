# Informe de code-review — US-029 "Gestor registra el cobro de la liquidación"

- Change: `us-029-registrar-cobro-liquidacion`
- Rama: `feature/us-029-registrar-cobro-liquidacion` (cambios en árbol de trabajo sobre `master` 498c2d6)
- Fecha: 2026-07-04 (re-revisión delta post-refinamiento: 2026-07-05)
- Revisor: code-reviewer (solo lectura)

## Alcance revisado
Dominio puro (`validar-cobro.ts`, `detectar-discrepancia.ts`, `puede-registrar-cobro.ts`),
use-case `registrar-cobro-liquidacion.use-case.ts`, adaptadores Prisma (UoW + repositorios),
controller `factura.controller.ts` (POST `/reservas/:id/facturas/liquidacion/cobro`),
DTOs `factura.dto.ts`, migración `20260704150000_us029_pago_tenant_id`, `schema.prisma`,
contrato `docs/api-spec.yml`, cliente generado `apps/web/src/api-client/schema.d.ts`,
tests unitarios de dominio y suite de concurrencia/integración.

## Guardrails DUROS

### Hexagonal — OK
- `domain/validar-cobro.ts`, `detectar-discrepancia.ts`, `puede-registrar-cobro.ts` NO tienen
  imports (0 `import`), sin `@nestjs/*`, `@prisma/*` ni `infrastructure/`.
- El use-case (application) depende solo de puertos inyectados (`UnidadDeTrabajoCobroPort`,
  `ClockPort`, repositorios tx-bound); no importa Prisma ni NestJS. Los adaptadores Prisma viven
  en `infrastructure/` e implementan los puertos.

### Bloqueo / atomicidad — OK
- Relectura de RESERVA con `SELECT ... FOR UPDATE` vía `$queryRaw` en
  `ReservaCobroPrismaRepository.releerConBloqueo` (lock de fila PostgreSQL).
- Toda la acción corre en un único `prisma.$transaction`; si el trabajo lanza, revierte
  (atomicidad estado<->PAGO).
- La guarda de doble cobro (`puedeRegistrarCobro`) se REEVALÚA DENTRO de la transacción tras el
  `FOR UPDATE`; `cobrada` -> 409 `LIQUIDACION_YA_COBRADA`.
- Sin Redis / Redlock / locks distribuidos ni locks en memoria.

### Multi-tenancy / RLS — OK
- `PAGO.tenant_id` añadido (schema + migración) con FK a TENANT e índices; RLS ya habilitada en
  `pago` desde `20260619190625_init`; la nueva policy `tenant_isolation` filtra por el `tenant_id`
  DIRECTO (`current_setting('app.tenant_id', true)`), coherente con el resto de tablas.
- La UoW fija el contexto con `fijarTenant(tx, tenantId)` (`SET LOCAL app.tenant_id`) como PRIMERA
  operación de la transacción.
- El `tenantId` deriva SIEMPRE del JWT (`@CurrentUser().tenantId`), nunca del path/body.
- Migración ADITIVA no destructiva: columna nullable -> backfill desde `factura.tenant_id` ->
  `SET NOT NULL`. Test de integración confirma que un justificante de OTRO tenant es invisible (404).

### Tipos / importes — OK
- Importes en `Decimal(10,2)` (schema `Pago.importe Decimal @db.Decimal(10,2)`;
  `new Prisma.Decimal(...)` al crear). El uso de `Number` es solo aritmética interna en céntimos
  enteros sobre strings Decimal; nunca se persiste como Float.
- DTOs validados con class-validator (`@IsString`+`@Matches(IMPORTE_PATTERN)`, `@IsDateString`,
  `@IsOptional`+`@IsUUID`).
- Arrow functions en todo el código nuevo; no hay `function` declarativo (solo métodos de clase
  NestJS, exentos).

### Contrato — OK
- `docs/api-spec.yml` añade la ruta y los schemas `RegistrarCobroLiquidacionRequest/Response`,
  `PagoLiquidacion`, `AlertaDiscrepanciaCobro`, `CobroLiquidacionError`. Códigos 200/400/401/403/
  404/409 coherentes con el mapeo del controller (`aHttp`). Discrepancia como campo OPCIONAL de la
  200 (no bloqueante), no como error.
- DTOs de NestJS reflejan los schemas del contrato.
- Cliente HTTP del frontend REGENERADO (cabecera "auto-generated by openapi-typescript", +195
  líneas); no editado a mano.

## Máquina de estados — OK
- Guarda modelada como tabla declarativa `GUARDA_COBRO` (Record por `liquidacion_status`), sin
  if/else disperso; estado desconocido -> bloqueo conservador. Solo avanza `liquidacion_status`
  (nunca `RESERVA.estado`, US-031 fuera de alcance), verificado en tests.

## Tests / idempotencia — OK
- Suite de concurrencia real: dos cobros concurrentes -> 1 OK + 1 rechazo, un ÚNICO PAGO,
  `factura.estado='cobrada'`, `liquidacion_status='cobrada'`, `RESERVA.estado` sin cambios.
- Cubre: happy path, justificante vinculado, precondición `pendiente` (409 sin PAGO), doble cobro
  secuencial, importe no positivo (400), fecha futura (400), justificante cross-tenant (404),
  discrepancia informativa con importe real. QA: 5 suites/50 tests verdes; 10/10 curl con estado de
  BD verificado; RLS confirmado (reports N+1 y N+2 presentes).

## Convenciones — OK
- Nombres en español (PascalCase/camelCase/kebab-case); errores y mensajes en español; comentarios
  con trazabilidad a US/UC/design.

## Frontend responsive
- No aplica: US-029 es backend + contrato + regeneración del cliente. Sin cambios de UI en `apps/web`
  salvo el cliente generado. No se requiere evidencia de 3 viewports.

## Observaciones no bloqueantes (estado)
- (Baja) [RESUELTA en delta 2026-07-05] `buscarJustificante` no filtraba por
  `tipo='justificante_pago'` ni por `reservaId`. Ahora el puerto recibe `reservaId` y el adaptador
  Prisma acota `idDocumento + tenantId + tipo=justificante_pago + reservaId`; un DOCUMENTO de otro
  tipo o de otra reserva se trata como NO encontrado → 404 `JUSTIFICANTE_NO_ENCONTRADO`. Ver
  §"Re-revisión delta".
- (Baja) [RESUELTA en delta 2026-07-05] La suite de integración usaba `rejects.toBeDefined()`.
  Ahora asevera la clase de error concreta (`rejects.toBeInstanceOf(...)`) y añade un caso que
  verifica el 404 para justificante de tipo incorrecto y para justificante de otra reserva.
- (Info) Deuda pre-existente ajena a US-029: deadlock 40P01 de US-004 y contaminación de numeración
  en suites US-022/US-028; no imputable a este change. Sigue vigente.

## Re-revisión delta (2026-07-05)

Delta revisado sobre el informe APTO previo. Aborda las 2 observaciones no bloqueantes; no
introduce regresiones de guardrails.

### Cambios revisados
- `application/registrar-cobro-liquidacion.use-case.ts`: el puerto `DocumentosCobroPort.buscarJustificante`
  ahora exige `reservaId` en sus params; el paso (5) del use-case lo invoca con
  `{ idDocumento, tenantId: comando.tenantId, reservaId: comando.reservaId }` y mapea `null` → 404.
- `infrastructure/cobro-liquidacion-repository.prisma.adapter.ts`:
  `DocumentoCobroPrismaRepository.buscarJustificante` añade al `findFirst` las cláusulas
  `tipo = TipoDocumentoPrisma.justificante_pago` y `reservaId`, además de `idDocumento`+`tenantId`.
- `__tests__/registrar-cobro-concurrencia.spec.ts`: aserciones endurecidas de `rejects.toBeDefined()`
  a `rejects.toBeInstanceOf(<clase concreta>)` (`LiquidacionNoFacturadaError`, `LiquidacionYaCobradaError`,
  `CobroInvalidoError`, `JustificanteNoEncontradoError`) y nuevo caso
  `debe_rechazar_un_justificante_de_tipo_incorrecto_o_de_OTRA_reserva_con_404` (tipo `presupuesto`
  mismo tenant/reserva → 404; `justificante_pago` mismo tenant pero otra reserva → 404), ambos con
  `contarPagosDeFactura === 0`.

### Guardrails re-verificados sobre el delta — OK
- **Hexagonal**: el puerto endurecido vive en `application/` (interfaz pura, 0 imports de infra); el
  filtro por `tipo`/`reservaId` se implementa en el adaptador `infrastructure/`. Dominio puro intacto.
- **Coherencia con el modelo**: `Documento.reservaId String?` y `tipo TipoDocumento` (con
  `justificante_pago` y `presupuesto`) existen en `schema.prisma` (enum líneas ~123-131, modelo ~522-535)
  y en la migración `20260619190625_init`. El filtro por `reservaId` es coherente con `DOCUMENTO.reservaId`.
- **Multi-tenancy / RLS**: la RLS `tenant_isolation` sobre `documento` sigue activa (init SQL L526-527);
  el nuevo filtro `reservaId`+`tipo` es defensa en profundidad SOBRE el RLS, no lo sustituye. El
  `tenantId` sigue derivando del JWT (`comando.tenantId`), nunca del path/body.
- **Atomicidad / FOR UPDATE**: sin cambios; la relectura `SELECT ... FOR UPDATE` de RESERVA y la
  reevaluación de la guarda bajo el lock permanecen intactas. Sin Redis/locks distribuidos.
- **Contrato**: `justificanteDocId` es un campo de request opcional que NO transporta `reservaId`
  (éste llega por el path `:id`); endurecer el filtro no altera el shape del request/response.
  `docs/api-spec.yml` y el cliente generado `apps/web/src/api-client/schema.d.ts` NO se tocaron en el
  delta (mtime 23:24 anterior al delta 00:54; `git diff --stat` sin cambios respecto a la revisión previa).
- **Arrow functions / tipos**: código nuevo con arrow functions; sin `function` declarativo; sin `any`
  (la única aparición `expect.any(Function)` es una aserción de Jest, no un tipo). TS strict.
- **Tests primero / precisión**: aserciones ahora por clase concreta de error; nuevo caso de tipo/reserva
  incorrectos. Verificación reportada por QA: 5 suites / 54 tests verdes, `eslint` EXIT 0,
  `tsc --noEmit` EXIT 0.

### Bloqueantes nuevos
- Ninguno.

## Veredicto: APTO
