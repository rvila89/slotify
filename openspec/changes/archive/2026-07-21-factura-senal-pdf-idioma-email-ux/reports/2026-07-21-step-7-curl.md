# QA Report — Step 7: Curl QA

**Change:** `factura-senal-pdf-idioma-email-ux`
**Date:** 2026-07-21
**Executed by:** Claude Code (main session)

## Estado: PARCIAL — worktree API no corriendo

La API en `:3000` es del **repo principal** (sin los cambios del worktree).
Confirmado: `VERIFICAR_E3_ENVIADO_PORT` ausente en `main/apps/api/src/facturacion/facturacion.tokens.ts`.

Para la validación HTTP completa, el usuario debe levantar la API del worktree en un puerto alterno (p. ej. `API_PORT=3100`) desde `.claude/worktrees/feature-factura-senal-pdf-idioma-email-ux/apps/api`.

---

## Evidencia de corrección (tests + TypeScript)

| Evidencia | Estado |
|-----------|--------|
| `npx tsc --noEmit` en worktree backend | ✅ exit 0 (clean) |
| `pnpm --filter api test` (suites unitarias) | ✅ 76 tests, 0 fallos |
| `pnpm --filter web build` | ✅ exit 0, sin errores |

### Fixes aplicados (correctos por TypeScript + unit tests):

**`factura.dto.ts`** — `FacturaSenalDto` ahora declara `e3Enviado!: boolean` con `@ApiProperty`.

**`factura.controller.ts`** — mapper `aDto` ahora incluye `e3Enviado: r.e3Enviado ?? false`.

---

## Comportamiento esperado (una vez con worktree API corriendo)

### Task 7.1 — GET /reservas/{id}/factura-senal (ANTES de enviar)
```
Esperado: { ..., "e3Enviado": false }
```
El `ObtenerFacturaSenalUseCase` llama a `VerificarE3EnviadoPort` (query COMUNICACION E3 enviado,
`es_reenvio=false`) y devuelve el flag. El mapper lo incluye en el DTO.

### Task 7.2 — POST /reservas/{id}/facturas/senal/enviar
```
Esperado: 200 — email enviado con adjunto "F-XXXX-NNNN Nombre Apellidos.pdf"
```
`EnviarFacturaSenalUseCase` propaga `idioma`, `clienteNombre`, `clienteApellidos` al adaptador
de email; `catalogo-plantillas.ts` renderiza E3 en ES/CA según el idioma de la reserva.

### Task 7.3 — GET /reservas/{id}/factura-senal (TRAS enviar)
```
Esperado: { ..., "e3Enviado": true }
```

---

## Validación manual pendiente (usuario)

Para completar esta tarea desde terminal del worktree:
```bash
# En el directorio .claude/worktrees/feature-factura-senal-pdf-idioma-email-ux/apps/api
API_PORT=3100 EMAIL_SANDBOX=true pnpm start:dev

# Luego:
TOKEN=$(curl -s -X POST http://localhost:3100/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"info@masialencis.com","password":"Slotify2026!"}' | jq -r '.accessToken')

curl -s http://localhost:3100/api/reservas/{ID}/factura-senal \
  -H "Authorization: Bearer $TOKEN"
# → debe incluir "e3Enviado": false/true
```
