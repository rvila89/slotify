# Step 6 — Tests unitarios + verificación de BD

**Fecha:** 2026-07-18
**Change:** fix-borrador-e1-cuerpo-prerelleno
**Branch:** feature/fix-borrador-e1-cuerpo-prerelleno (worktree aislado)

---

## Comandos y resultados

### Typecheck
```
npx tsc --noEmit
```
**Resultado:** ✅ sin errores.

### Lint (archivos tocados)
```
npx eslint src/reservas/application/alta-consulta.use-case.ts \
  src/reservas/reservas.module.ts \
  src/comunicaciones/application/despachar-email.service.ts \
  src/comunicaciones/infrastructure/comunicacion.repository.prisma.adapter.ts \
  src/comunicaciones/domain/comunicacion.repository.port.ts \
  src/reservas/__tests__/alta-consulta-borrador-e1-cuerpo.use-case.spec.ts
```
**Resultado:** ✅ EXIT 0 (sin warnings; `func-style`/`prefer-arrow-callback` respetados; helper `renderizarE1` como método de clase).

### Suite del fix (nueva)
```
npx jest alta-consulta-borrador-e1-cuerpo
```
**Resultado:** ✅ 1 suite, **9 tests passed**. Cubre: relleno de asunto+cuerpo renderizados sin enviar; idioma seleccionado (`ca`); casuística `tipoE1` (`sin_fecha`/`fecha_disponible`/`fecha_cola`/`fecha_confirmada` con fechas alternativas); best-effort (fallo del UPDATE no tumba el alta); no-UPDATE en auto-envío; paridad de cuerpo con el auto-envío.

### Suites afectadas (regresión)
```
npx jest alta-consulta.use-case despachar-email.service comunicacion.repository.port comunicacion.listar-por-reserva
```
**Resultado:** ✅ 5 suites, **67 tests passed** (incluye el fix).

### Batería completa unitaria
```
npx jest
```
**Resultado:** **2410 passed**, 11 failed en 7 suites — **todas pre-existentes y ajenas al fix**:
- `expirar-consultas-integracion`, `confirmar-pago-senal-integracion`, `programar-visita-concurrencia`: requieren Postgres (no cuentan como unit; se cubren en Step 7).
- 4× `documento-*.plantilla`: flakiness ESM de react-pdf al correr las suites juntas (deuda documentada, pre-existente).

**Veredicto:** PASS — sin regresiones atribuibles al fix.
