# Step 8 — Pruebas manuales curl (end-to-end, BD real)

**Fecha:** 2026-07-18
**Change:** fix-borrador-e1-cuerpo-prerelleno
**Setup:** API NestJS en `http://localhost:3000` (prefix `/api`), `slotify_dev`,
`EMAIL_TRANSPORT=fake`. Usuario: `info@masialencis.com` / `Slotify2026!` (gestor, tenant
`00000000-0000-0000-0000-000000000001`). Datos QA creados y **eliminados** al final
(baseline restaurada).

---

## Test 1 — Login
`POST /api/auth/login` → **HTTP 200**, `accessToken` (316 chars).

## Test 2 — Alta CON comentarios (idioma `ca`, sin fecha) → borrador redactado
`POST /api/reservas` con `comentarios` + `idioma:"ca"` → reserva creada (`26-0001`).
`GET /api/reservas/{id}/comunicaciones`:

```
codigo: E1 | estado: borrador
asunto: "Hem rebut la teva consulta — Masia l'Encís"
cuerpoLen: 1452
cuerpo[0..80]: "<p>Hola QAFix,</p><p>Moltes gràcies pel teu interès en la Masia l'Encís! 😊<"
```

**Verdict:** ✅ PASS — **el bug queda corregido**: el borrador nace con el **asunto
renderizado en catalán** (no el placeholder) y el **cuerpo NO vacío** (1452 chars,
variante `sin_fecha` en catalán). Antes del fix `cuerpo` era `''`.

## Test 3 — Alta SIN comentarios (auto-envío) → sin regresión
`POST /api/reservas` sin `comentarios` → `GET .../comunicaciones`:

```
codigo: E1 | estado: enviado | fechaEnvio: 2026-07-18T15:01:53.844Z
```

**Verdict:** ✅ PASS — el auto-envío sigue promoviendo el E1 a `enviado` con `fecha_envio`
(sin regresión; la extracción del helper de render preserva el camino de envío).

## Limpieza
`deleteMany` de las 2 comunicaciones, 2 reservas y 2 clientes de prueba →
`slotify_dev` restaurada a su estado previo.

**Veredicto global:** PASS.
