# Step 8 — E2E con Playwright MCP
**Change:** condiciones-idioma-e2-firma-banner
**Fecha:** 2026-07-21
**Outcome:** PENDIENTE DE ENTORNO (frontend y API no arrancados)

---

## Estado del entorno

```bash
curl -s http://localhost:5173  # → sin respuesta (Vite dev server no arrancado)
curl -s http://localhost:4173  # → sin respuesta (preview mode no arrancado)
curl -s http://localhost:3000/health  # → sin respuesta (API NestJS no arrancada)
```

Ni el frontend (Vite/React) ni el backend (NestJS) están corriendo. Los flujos E2E no se pudieron ejecutar con Playwright MCP.

---

## Flujos que se habrían verificado

### Viewport 1280 (escritorio)

**Flujo 1 — Banner de firma (Mejora C)**
1. Navegar a reserva en estado `reserva_confirmada` con `condPartFirmadas=false` y `condPartFechaEnvio!=null`
2. Localizar la tarjeta "Firma de condiciones particulares"
3. Verificar que muestra alerta ámbar "pendiente" (no el texto de condiciones no enviadas)
4. Hacer clic en "Registrar condiciones firmadas" → se abre `RegistrarFirmaDialog`
5. Subir fichero de prueba → confirmar registro
6. Verificar que aparece banner verde `[data-testid="aviso-condiciones-registradas"]` en la cabecera
7. Verificar que NO aparece toast de Sonner
8. Verificar que el scroll sube al top de la página

**Flujo 2 — Texto "(E2)" en tarjeta (Mejora C)**
1. Navegar a reserva en estado `pre_reserva` (donde aún no se mostraría la sección de condiciones) o en `reserva_confirmada` con `condPartFechaEnvio=null`
2. Verificar que el mensaje de "condiciones no enviadas" incluye el texto "(E2)" y NO "(E3)"
   - Selector: `[data-testid="condiciones-no-enviadas"]`
   - Texto esperado: contiene "E2" (no "E3")

**Flujo 3 — Re-subir firma: banner diferenciado**
1. Navegar a reserva con `condPartFirmadas=true`
2. Hacer clic en "Subir nueva versión firmada"
3. Subir nuevo fichero → confirmar
4. Verificar que aparece banner `[data-testid="aviso-condiciones-reregistradas"]` (tipo `'reregistrada'`)
5. Verificar que el texto dice "Nueva versión registrada"

### Viewport 768 (tablet)

- Mismo flujo completo sin overflow horizontal
- Sidebar de navegación colapsa a drawer + hamburguesa (breakpoint `<lg`)
- Botón de firma `w-full` visible y con 48px de alto mínimo

### Viewport 390 (móvil)

- Mismo flujo completo sin overflow horizontal
- Banner inline `AvisoCondicionesFirmadas` fluye correctamente (flex items-start)
- Botón de cerrar accesible a la derecha del banner
- Tarjeta de condiciones sin overflow en 390px

---

## Comandos Playwright que se habrían ejecutado

```javascript
// Viewport 1280
await browser_resize({ width: 1280, height: 900 });
await browser_navigate({ url: 'http://localhost:5173' });
// Login y navegar a reserva...
await browser_snapshot();  // verificar tarjeta condiciones

// Verificar texto E2
const snapshot = await browser_snapshot({ target: '[data-testid="condiciones-no-enviadas"]' });
// assert: snapshot contiene "(E2)"

// Registrar firma
await browser_click({ target: '[data-testid="accion-registrar-firma-condiciones"]' });
// ... upload + confirm ...
await browser_snapshot({ target: '[data-testid="aviso-condiciones-registradas"]' });
// assert: banner verde visible, no toast Sonner

// Viewport 390
await browser_resize({ width: 390, height: 844 });
// Repetir flujo, verificar no overflow
await browser_take_screenshot({ type: 'png', scale: 'css' });
```

---

## Capturas

Directorio preparado: `reports/e2e-screenshots/` (vacío — entorno no disponible)

---

## Verificación estática del componente (compensatoria)

Aunque no se pudo ejecutar E2E real, se verificó el código fuente:

**`CondicionesFirmadasCard.tsx` línea 85:**
```tsx
cliente (E2).
```
El texto "(E2)" está presente en el mensaje de condiciones no enviadas. Antes era "(E3)".

**`AvisoCondicionesFirmadas.tsx`:**
- Banner con `role="status"` y `data-testid="aviso-condiciones-registradas"` / `"aviso-condiciones-reregistradas"`
- Clases Tailwind mobile-first: `flex items-start gap-3`, sin anchuras fijas
- Botón de cerrar `shrink-0` con `aria-label="Cerrar aviso"`

**`CondicionesFirmadasCard.tsx` bifurcación `onRegistrado`:**
```tsx
if (onRegistrado) {
  onRegistrado(firmadas ? 'reregistrada' : 'registrada');
  return;  // NO llama notify.success()
}
notify.success(...)  // solo si no se pasa onRegistrado (fallback)
```

Tests unitarios que cubren esto (3 tests PASS):
- `al_registrar_con_exito_invoca_onRegistrado_con_registrada_cuando_no_estaba_firmada`
- `al_registrar_con_exito_invoca_onRegistrado_con_reregistrada_cuando_ya_estaba_firmada`
- `NO_emite_notify_success_cuando_se_pasa_onRegistrado`

---

## Outcome: PENDIENTE

Los flujos E2E completos están pendientes. La verificación estática y los tests unitarios cubren la lógica de los 3 flujos, pero no sustituyen la prueba E2E con Playwright en los 3 viewports obligatorios. Se deberá re-ejecutar este step una vez el entorno esté disponible y los fallos de Step 6 estén corregidos.
