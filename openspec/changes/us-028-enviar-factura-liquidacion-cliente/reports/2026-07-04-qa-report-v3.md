# QA Report v3 — US-028 "Gestor aprueba y envía la factura de liquidación"

Fecha: 2026-07-04
Rama: feature/us-028-enviar-factura-liquidacion-cliente
BD dev aislada: slotify_dev (fixtures QA creados y eliminados al terminar)

---

## Tests backend: PASS (206/206 dirigidos; 1189/1189 suite completa)

### Comandos ejecutados

```
cd apps/api
npx jest --testPathPatterns="facturacion" --runInBand     # 153 passed, 15 suites
npx jest --testPathPatterns="comunicaciones" --runInBand  # 53 passed, 10 suites
pnpm --filter @slotify/api run test                       # 1189 passed, 133 suites
```

### Resultados por suite (dirigida)

| Suite | Tests | Estado |
|---|---|---|
| facturacion/domain (aplicar-descuento, total-liquidacion) | 13 | PASS |
| aprobar-y-enviar-liquidacion.use-case.spec (unidad) | 23 | PASS |
| enviar-recibo-fianza-separado.use-case.spec (unidad) | 11 | PASS |
| reenviar-liquidacion.use-case.spec (unidad) | 10 | PASS |
| aprobar-y-enviar-atomicidad.spec (integración) | 5 | PASS |
| aprobar-y-enviar-concurrencia.spec (integración) | 4 | PASS |
| comunicaciones (e4-liquidacion-fianza, despachar-email) | 53 | PASS |
| resto facturacion (módulo, calculo, máquina estados, numeración) | ~87 | PASS |
| **Total dirigido** | **206** | **PASS** |

Suite completa `pnpm test`: **1189/1189** passed. El error de salida (exit code 1) se debe a que `pnpm run test` encadena `depcruise` con los mismos args de jest (comportamiento preexistente, no relacionado con US-028).

### Estado BD (slotify_test) pre/post tests

Los tests de integración usan `deleteMany` en `afterEach` sobre `slotify_test`. Tests unitarios: mocks en memoria, sin BD. Sin mutación residual.

---

## Build frontend: PASS

### Comandos ejecutados

```
pnpm --filter @slotify/web run typecheck   # tsc --noEmit: sin errores
pnpm --filter @slotify/web run build       # vite build: built in 7.31s
```

### Resultado

```
✓ built in 7.31s
dist/index.html  0.48 kB
dist/assets/index-Df86xtG0.js  818.12 kB (warning chunk size >500 kB, pre-existente)
```

`AccionesFacturacion` exportado del barrel `apps/web/src/features/facturacion/index.ts` (línea 10).
`DocumentosLiquidacionFianza` importado y montado en `FichaConsultaPage.tsx` (líneas 14, 244-248).
Los tres nuevos use cases inyectados en `FacturaController` (constructor líneas 168-175).

---

## Curls manuales: PASS

### Estado del servidor

API corriendo en `http://localhost:3000` (servidor dev preexistente, reuseExistingServer).
Credenciales: `info@masialencis.com` / `Slotify2026!`.

### Fixtures QA

Creados: `RESERVA_ID=a0280000-0000-0000-0000-000000000002` y `RESERVA2_ID=a0280001-0000-0000-0000-000000000002` con facturas de liquidación y fianza en borrador (`slotify_dev`).

### Resultados

| Test | Comando | Respuesta | HTTP | Estado |
|---|---|---|---|---|
| A. Happy path aprobar-enviar | `POST /api/reservas/{R1}/facturas/liquidacion/aprobar-enviar` body `{}` | `{liquidacion:{estado:"enviada", numeroFactura:"F-2026-0001"}, fianza:{estado:"enviada", numeroFactura:"F-2026-0002"}, liquidacionStatus:"facturada", fianzaStatus:"recibo_enviado"}` | 200 | PASS |
| B. 409 ya enviada | 2.º POST mismo endpoint | `{codigo:"FACTURA_NO_BORRADOR", error:"Conflict"}` | 409 | PASS |
| C. Reenvío (D-4) | `POST /api/reservas/{R1}/facturas/liquidacion/reenviar` | `{liquidacion:{numeroFactura:"F-2026-0001", estado:"enviada"}, comunicacion:{estado:"enviado"}}` numero_factura intacto, nueva COMUNICACION E4 | 200 | PASS |
| D. Sin auth 401 | `POST /api/reservas/{R1}/facturas/liquidacion/aprobar-enviar` sin Bearer | `{statusCode:401, error:"Unauthorized"}` | 401 | PASS |
| E. 404 reserva inexistente | `POST /api/reservas/ffffffff-0000-0000-0000-000000000000/facturas/liquidacion/aprobar-enviar` | `{codigo:"FACTURA_LIQUIDACION_NO_ENCONTRADA", error:"Not Found"}` | 404 | PASS |
| F. 409 fianza ya enviada | `POST /api/reservas/{R1}/facturas/fianza/enviar` (fianza ya en enviada) | `{codigo:"FACTURA_NO_BORRADOR", error:"Conflict"}` | 409 | PASS |
| G. Fianza separado happy path | `POST /api/reservas/{R2}/facturas/fianza/enviar` (fianza en borrador) | `{fianza:{estado:"enviada", numeroFactura:"F-2026-0003"}, fianzaStatus:"recibo_enviado"}` | 200 | PASS |

### Comparación BD pre/post

| tabla | pre | post (R1) | restaurado |
|---|---|---|---|
| RESERVA (liquidacionStatus) | pendiente | facturada | sí (DELETE) |
| RESERVA (fianzaStatus) | pendiente | recibo_enviado | sí |
| FACTURA liquidacion (estado) | borrador | enviada | sí |
| FACTURA fianza (estado) | borrador | enviada | sí |
| FACTURA liquidacion (numeroFactura) | null | F-2026-0001 | sí |
| COMUNICACION count | 0 | 3 (1 aprobar + 2 reenvíos) | sí |

### Restauración

```js
prisma.comunicacion.deleteMany({ where: { reservaId: R1 } })  // 4 filas
prisma.factura.deleteMany({ where: { reservaId: R1 } })       // 4 filas
prisma.reserva.deleteMany({ where: { idReserva: R1 } })       // 2 filas
prisma.cliente.deleteMany({ where: { idCliente: C } })        // 1 fila
// DB final: solo reserva consulta E2E preexistente
```

### Outcome curl
PASS

---

## Playwright E2E: PASS

### Setup

API: `http://localhost:3000` (dev preexistente). Frontend: `http://localhost:5173` (dev preexistente).
Fixture E2E: `RESERVA_ID=e2efc028-0000-0000-0000-000000000002` (`reserva_confirmada`, borradores de liquidación y fianza).

### Flujo ejecutado

1. Login: `info@masialencis.com` / `Slotify2026!` → redirect a `/calendario`.
2. SPA navigate a `/reservas/e2efc028-0000-0000-0000-000000000002`.
3. Verificar `[data-testid="acciones-facturacion"]` visible.
4. Verificar `[data-testid="accion-aprobar-enviar"]` presente y habilitado (disabled=false).
5. Click en "Aprobar y enviar factura" → `[role="dialog"]` se abre.
6. Dialog muestra desglose: base 496,69 €, IVA 21% 104,31 €, total 601,00 €.
7. Campos opcionales: descuento y motivo disponibles.

### Verificación responsive

| Viewport | Panel AccionesFacturacion visible | Botón activo | Overflow horizontal | Nav comportamiento | Resultado |
|---|---|---|---|---|---|
| 390 (móvil) | sí | sí | no | hamburger (≡) colapsado a drawer | PASS |
| 768 (tablet) | sí | sí | no | navegación colapsada (<lg) | PASS |
| 1280 (escritorio) | sí | sí | no | sidebar fijo visible (≥lg) | PASS |

### Screenshots

- `e2e-028-1280.png` — sidebar + ficha reserva_confirmada desktop
- `e2e-028-1280-dialog.png` — dialog "Aprobar y enviar la factura de liquidación"
- `e2e-028-390.png` — hamburger + ficha mobile
- `e2e-028-768.png` — tablet layout

### Restauración

Fixture E2E eliminado: `e2efc028-*` (2 facturas, 1 reserva, 1 cliente).
BD final: solo reserva consulta E2E preexistente (1 row).

### Outcome E2E
PASS

---

## Resumen

| Verificación | Resultado |
|---|---|
| Tests backend (facturacion + comunicaciones dirigidos) | PASS 206/206 |
| Suite completa pnpm test | PASS 1189/1189 |
| Frontend typecheck | PASS |
| Frontend build | PASS |
| Curl happy path aprobar-enviar | PASS 200 |
| Curl 409 FACTURA_NO_BORRADOR | PASS 409 |
| Curl reenvío (D-4 nueva COMUNICACION) | PASS 200 |
| Curl 401 sin auth | PASS 401 |
| Curl 404 reserva inexistente | PASS 404 |
| Curl fianza separado happy path | PASS 200 |
| E2E panel AccionesFacturacion visible | PASS |
| E2E dialog "Aprobar y enviar" se abre | PASS |
| E2E responsive 390 móvil | PASS |
| E2E responsive 768 tablet | PASS |
| E2E responsive 1280 escritorio | PASS |
| Restauración BD | PASS |

## Veredicto: APTO
