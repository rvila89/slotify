# Step 11 — Unit Tests + DB State Verification

**Date:** 2026-07-17
**Change:** us-047-borrador-e1-comportamiento
**Branch:** feature/us-047-borrador-e1-comportamiento

---

## DB State (Baseline — before tests)

| Table | Count |
|---|---|
| reserva | 13 |
| cliente | 5 |
| comunicacion | 28 |
| fecha_bloqueada | 5 |

---

## Suite 1 — `enviar-borrador` (EnviarBorradorUseCase + dossier E1)

**Command:**
```
cd apps/api && npx jest --testPathPatterns="enviar-borrador" --no-coverage
```

**Result:** PASS — 23 tests, 0 failed, 0 skipped

Covers:
- 3.1 Confirmar envío sin editar (3 tests)
- 3.2 Edición opcional de asunto/cuerpo (3 tests)
- 3.3 Guarda de estado / idempotencia (4 tests)
- 3.4 Validación de destinatario previa (4 tests)
- 3.5 Fallo del proveedor (2 tests)
- 3.9 No encontrada / otro tenant (3 tests)
- US-047 Step 2: adjunto del dossier E1 según idioma (4 tests)

---

## Suite 2 — `listar-reservas` (tieneBorradorE1Pendiente en el pipeline)

**Command:**
```
cd apps/api && npx jest --testPathPatterns="listar-reservas" --no-coverage
```

**Result:** PASS — 46 tests across 5 suites, 0 failed, 0 skipped

Suites:
- `listar-reservas.use-case.spec.ts` — 21 tests (includes `tieneBorradorE1Pendiente` in `PipelineReservaLectura`)
- `listar-reservas.prisma.adapter.spec.ts` — unit adapter tests
- `listar-reservas.controller.http.spec.ts` — controller tests
- `listar-reservas-subestado-null-integracion.spec.ts` — integration tests

---

## Suite 3 — Full API suite (regression check)

**Command:**
```
cd apps/api && npx jest --no-coverage 2>&1 | tail -30
```

**Result:** 247 suites total — 233 passed, 14 failed (details below)

**Tests total:** 2412 — 2370 passed, 42 failed

### Failures breakdown

#### NEW REGRESSIONS introduced by US-047 (2 tests in 1 suite)

**Suite:** `src/comunicaciones/infrastructure/plantillas/catalogo-plantillas.spec.ts`
- `debe_declarar_las_variables_requeridas_de_la_plantilla_E1` — FAIL
  - Test expects `variablesRequeridas` to contain `'email'`, but US-047 changed E1 to `['nombre', 'tipoE1']`.
- `no_debe_tener_plantilla_en_un_idioma_no_provisto_para_que_el_motor_aplique_fallback` — FAIL
  - Test expects `seleccionar('E1', 'ca')` to return `null`, but US-047 added `PLANTILLA_E1_CA` (active E1 in Catalan).

**Impact:** These 2 tests in `catalogo-plantillas.spec.ts` (written for US-045) are now violated by the US-047 implementation. The spec file was NOT modified in this branch.

#### PRE-EXISTING failures (not introduced by US-047)

All other failures are pre-existing flaky integration/concurrency tests that fail under parallel execution but pass when run in isolation. Confirmed by:
1. None of these test files were modified in this branch (`git diff master` shows no changes)
2. Running them individually they all PASS

Pre-existing failing suites (fail in parallel run, pass in isolation):
- `documento-condiciones.plantilla.spec.ts` — react-pdf ESM `--experimental-vm-modules` issue (known)
- `documento-presupuesto.plantilla.spec.ts` — same react-pdf ESM issue (known)
- `documento-presupuesto-pie-bancario.plantilla.spec.ts` — same
- `documento-presupuesto-sin-iva.plantilla.spec.ts` — same
- `aprobar-y-enviar-atomicidad.spec.ts` — DB concurrency timeout (known)
- `enviar-factura-senal-integracion.spec.ts` — DB concurrency (known)
- `reenviar-e3-integracion.spec.ts` — DB concurrency (known)
- `expirar-consultas-integracion.spec.ts` — flaky in parallel (known)
- `confirmar-pago-senal-integracion.spec.ts` — flaky in parallel (known)
- `finalizar-evento-integracion.spec.ts` — flaky in parallel (known)
- `aprobar-y-enviar-concurrencia.spec.ts` — concurrency deadlock (known)
- `bloquear-fecha-integracion.spec.ts` — flaky in parallel (known)
- `liberar-fecha-integracion.spec.ts` — flaky in parallel (known)

---

## DB State (Post-tests)

| Table | Count |
|---|---|
| reserva | 13 |
| cliente | 5 |
| comunicacion | 28 |
| fecha_bloqueada | 5 |

**DB mutation:** None — counts identical to baseline. Tests are unit tests (mocked ports); no DB touched.

---

## Summary

| Suite | Tests | Status |
|---|---|---|
| enviar-borrador | 23 | PASS |
| listar-reservas (all) | 46 | PASS |
| Full suite (target suites) | 69 | PASS |
| Full suite (global) | 2412 | 42 FAILED (2 new regressions, 40 pre-existing) |

**Outcome: PARTIAL PASS**

The 69 targeted tests for US-047 all pass. However, 2 tests in `catalogo-plantillas.spec.ts` are regressions introduced by this change:
1. The implementation changed `variablesRequeridas` for E1 from `['email']` to `['nombre', 'tipoE1']` — the spec test expected `'email'` to be present.
2. The implementation added an active `PLANTILLA_E1_CA` — the spec test expected `seleccionar('E1', 'ca')` to return `null`.

The developer needs to update `catalogo-plantillas.spec.ts` to reflect the new US-047 behavior before this step can be marked green.
