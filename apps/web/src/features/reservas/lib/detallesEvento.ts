/**
 * Placeholder para datos opcionales AUSENTES en la ficha (US-051 §Punto 1). En vez
 * de omitir el campo, la ficha muestra este texto para que el gestor vea qué
 * información falta. `components/` aloja SOLO `.tsx` (regla dura): esta constante
 * vive en `lib/`.
 */
export const PLACEHOLDER_DATO_AUSENTE = 'De momento no se dispone de esta información';
